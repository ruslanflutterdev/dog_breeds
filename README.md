# 🐶 Dog Breeds

**Dog Breeds** — это мобильное приложение на Flutter, представляющее собой справочник пород собак. Приложение позволяет пользователям просматривать список популярных пород и открывать детальную информацию о каждой из них.

## ✨ Основные возможности

* **📜 Список пород**: Удобный список с карточками различных пород собак.
* **🔍 Детальный просмотр**: При нажатии на породу открывается отдельный экран с полноразмерным изображением и названием.
* **🖼️ Локальные ресурсы**: Все изображения хранятся внутри приложения (в папке `assets`), что обеспечивает быструю загрузку без интернета.

## 🛠 Технологический стек

* **Фреймворк**: [Flutter](https://flutter.dev/) (Dart)
* **Навигация**: Использование `Navigator` для переходов между экранами (`HomePage` -> `DogScreen`).
* **UI**: Material Design виджеты.

## 📂 Структура проекта

Проект имеет простую и понятную структуру:

```text
lib/
├── dog_screen.dart  # Экран с деталями (фото и название породы)
├── home_page.dart   # Главный экран со списком всех пород
└── main.dart        # Точка входа в приложение
assets/              # Папка с изображениями (png)
